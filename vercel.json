{
  "version": 2,
  "framework": "vite",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "routes": [
    { "handle": "filesystem" },
    { "src": "^/[^.]+(?:/[^.]+)*$", "dest": "/" }
  ]
}

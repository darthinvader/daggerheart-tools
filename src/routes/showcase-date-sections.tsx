import { createFileRoute } from '@tanstack/react-router';

// Placeholder to satisfy route scanner; actual section is lazy in routes/showcase.tsx
export const Route = createFileRoute('/showcase-date-sections')({
  component: () => null,
});
